import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Thomas Malacchina — AI Solutions Engineer",
  description:
    "Aerospace engineer specialized in complex B2B sales, passionate about deploying LLMs. I design and prototype solutions (API, n8n, Claude Code) to demonstrate AI ROI for Enterprise clients.",
  openGraph: {
    title: "Thomas Malacchina — AI Solutions Engineer",
    description: "AI Solutions Engineer & Go-To-Market Strategist based in Paris.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="min-h-screen antialiased">{children}</body>
    </html>
  );
}
